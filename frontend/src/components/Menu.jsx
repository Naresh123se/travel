import React from "react";

const Menu = () => {
  const mobile = (mobile) => {
    return <h1>mm</h1>;
  };

  const Desktop = (Desktop) => {
    return <h1>ll</h1>;
  };

  return (
    <div>
      <></>
    </div>
  );
};

export default Menu;
